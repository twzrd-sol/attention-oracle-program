# Core program (local development)

# Solana cluster URL used by Anchor / SDK / CLI
# Example (mainnet): https://api.mainnet-beta.solana.com
# Example (devnet):  https://api.devnet.solana.com
ANCHOR_PROVIDER_URL=

# Script guardrails (required for ops scripts)
# CLUSTER must be one of: localnet | devnet | testnet | mainnet-beta
CLUSTER=
# RPC URL required by scripts (can be same as ANCHOR_PROVIDER_URL)
RPC_URL=
# Explicit keypair path required by scripts
KEYPAIR=
# Mainnet safety latch (set to 1 to allow mainnet ops)
I_UNDERSTAND_MAINNET=

# Optional override for CLI/SDK RPC (falls back to ANCHOR_PROVIDER_URL)
AO_RPC_URL=

# Local wallet keypair used for deploys and tests
# Default for Solana CLI is usually: ~/.config/solana/id.json
ANCHOR_WALLET=

# Program configuration
# See DEPLOYMENTS.md for current program IDs.
AO_PROGRAM_ID=

# Token-2022 mint for your deployment (optional, used by scripts)
ATTENTION_MINT=

# HTTP port for the demo server
PORT=3000

# Cluster for Switchboard feed (devnet | mainnet-beta | testnet)
SB_CLUSTER=devnet

# Switchboard aggregator public key
SB_FEED=


# Usage
# 1. Copy this file to .env
# 2. Fill in values for your environment (do not commit .env)
#
#   cp .env.example .env
