[workspace]
members = [
    "programs/token-2022",
    "bearer/programs/bearer-claims",
    "bearer/programs/epoch-root-registry",
    "probe-harness",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
