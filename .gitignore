# Dependencies
node_modules/
*/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# Next.js
.next/
out/
build/
dist/

# Exclude app subtrees by default (will add allow-list when ready)
apps/*
apps/stream-listener-ts/
apps/twzrd-aggregator-rs/
portal-v3/
x402-api-server/

# Environment files (never commit secrets)
.env
.env.*.local
.env*
**/.env*
# but allow tracked examples across the repo
!.env.example
!**/.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Rust/Anchor
target/
**/*.rs.bk

# Test artifacts
test-ledger/
.anchor/

# Logs
logs/
*.log

# Keys (CRITICAL - Never commit keys!)
*.json
!package*.json
!tsconfig.json
*.pem
*.key
.keys/

# Common key/config directories
keys/

# Database
*.db
*.sqlite

# OS files
Thumbs.db
.DS_Store

# Repo hygiene (avoid committing internal or binary artifacts)
backups/
claim-ui-export/
verify-snapshot/
test-ledger/
submission_clean_*.zip
gateway/
infra/
db/
tmp_channels.txt
.cache/

# Internal deployment docs (keep private)
DEPLOYMENT_STATUS.md
**/DEPLOYMENT_STATUS.md

# Private apps – never commit
apps/twzrd-aggregator/
apps/portal-*/
apps/claim-ui/
apps/worker-v2/
apps/ctw-*/
apps/_deprecated-*/
apps/_archive/
# Public repo scope (only program + oracle)
docs/
cli/
sdk/
scripts/
apps/twzrd-aggregator-rs/
apps/stream-listener-ts/
schema_dump*.sql
twzrd-stream-listener-*

.claude/
GILL_MIGRATION_DIRECTIVE.md
