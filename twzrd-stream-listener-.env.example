# ============================================================================
# Stream Listener Configuration
# ============================================================================

# Solana RPC Configuration
# Use Helius, QuickNode, or your own Solana validator
ANCHOR_PROVIDER_URL=https://api.mainnet-beta.solana.com

# Optional: Override RPC for SDK
AO_RPC_URL=

# Optional: WebSocket endpoint for faster event streaming
AO_RPC_WS=wss://api.mainnet-beta.solana.com

# Program ID (must match deployed program on mainnet)
# Default: GnGzNdsQMxMpJfMeqnkGPsvHm8kwaDidiKjNU2dCVZop (v0.2.1-clean)
AO_PROGRAM_ID=GnGzNdsQMxMpJfMeqnkGPsvHm8kwaDidiKjNU2dCVZop

# ============================================================================
# Redis / BullMQ Configuration
# ============================================================================

# Redis connection string
REDIS_URL=redis://localhost:6379

# Redis host (alternative to REDIS_URL)
REDIS_HOST=localhost
REDIS_PORT=6379

# BullMQ queue prefix (namespaces all queues)
BULLMQ_PREFIX=twzrd

# ============================================================================
# Logging Configuration
# ============================================================================

# Log level: debug, info, warn, error
LOG_LEVEL=info

# Logs directory (relative to cwd)
LOG_DIR=./logs

# Node environment (development, production)
NODE_ENV=production

# ============================================================================
# Stream Listener Configuration
# ============================================================================

# Solana commitment level: processed, confirmed, finalized
STREAM_COMMITMENT=confirmed

# ============================================================================
# Optional: Integration with Aggregator
# ============================================================================

# If aggregator is running separately, can push events here
AGGREGATOR_URL=http://localhost:8000

# Auth token for aggregator push (if required)
AGGREGATOR_TOKEN=

# ============================================================================
# Monitoring & Observability
# ============================================================================

# Metrics endpoint (if using Prometheus)
METRICS_ENDPOINT=http://localhost:9090/metrics

# Health check endpoint port (optional)
HEALTH_CHECK_PORT=
