# Worker Configuration
WORKER_ID=worker-0
SHARD_INDEX=0
TOTAL_SHARDS=1

# Channel list (comma-separated, lowercase, no #)
# For 100+ channels, use fleet mode
CHANNELS=lacy,ninja,xqc,shroud,pokimane

# Health check
HEALTH_PORT=8080

# Logging
LOG_LEVEL=info

# Optional: Coordinator URL (for centralized aggregation)
COORDINATOR_URL=http://localhost:3000
EPOCH_SECONDS=60
